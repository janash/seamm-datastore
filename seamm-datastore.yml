name: seamm-datastore
channels:

  - conda-forge

  - defaults
dependencies:
  - python>=3.8
  - pip
  - pip:
    - flask
    - flask-authorize
    - sqlalchemy
    - marshmallow_sqlalchemy
    # For testing:
    - pytest
    - pytest-cov
    - pytest-mock
    # For development
    - black
    - flake8
    - twine


    # Pip-only installs
  #- pip:
  #  - codecov

